/**
 * @param {number} n
 * @return {number}
 * 
 * Count the number of prime numbers less than a non-negative number `n`
 */

const countPrimes = n => {
  // let count = 0
  // iterate over range(n) checking if isPrime(i)
    // if true, increment count
  // return count
};

console.log(countPrimes(12))
