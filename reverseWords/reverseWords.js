/**
 * @param {string} str
 * @returns {string}
 */
const reverseWords = str => {
  let parts = str.trim()
  let reversed = ''
  for (let i = str.length - 1; i > 0; i--) {}
  return reversed
}

let string = ' 3  the sky is blue   '
string.trim() /*?*/
string
// let test = reverseWords(string) // -> "blue is sky the"
// test
